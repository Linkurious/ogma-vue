module.exports = {
  componentsRoot: 'src/', // the folder where CLI will start searching for components.
  // components: '**/*.vue', // the glob to define what files should be documented as components (relative to componentRoot)
  components: 'layers/Test-Compo.vue',
  outDir: 'docs/src/test',
}